## Навигация
- [Требования](./Требования.md)

## ✅ Чек-лист прогресса реализации

### ЧАСТЬ 1. Разработка сервиса

#### 1. Управление клиентами (Получателями)
- [x] Модель `Client` создана
  - [x] Поля: email (unique), full_name, comment
  - [x] Поле `owner` (FK → CustomUser)
  - [x] Permissions в Meta: `can_view_all_recipients`
- [x] CRUD views реализованы
  - [x] `ClientListView` - список получателей
  - [x] `ClientDetailView` - детали получателя
  - [x] `ClientCreateView` - создание (с `form_valid()` для owner)
  - [x] `ClientUpdateView` - редактирование
  - [x] `ClientDeleteView` - удаление
- [x] Формы созданы (`ClientForm`)
- [x] URL patterns настроены
- [x] Шаблоны созданы (list, detail, form, confirm_delete)
- [x] **Добавить `LoginRequiredMixin` во все views**
- [x] **Добавить фильтрацию по owner в `get_queryset()`**
- [x] **Добавить `UserPassesTestMixin` в Update/Delete views**

#### 2. Управление сообщениями
- [x] Модель `MessageTemplate` создана
  - [x] Поля: subject, body
  - [x] Поле `owner` (FK → CustomUser)
  - [x] Permissions в Meta: `can_view_all_messages`
- [x] CRUD views реализованы
  - [x] `MessageTemplateListView` - список сообщений
  - [x] `MessageTemplateDetailView` - детали сообщения
  - [x] `MessageTemplateCreateView` - создание (с `form_valid()`)
  - [x] `MessageTemplateUpdateView` - редактирование
  - [x] `MessageTemplateDeleteView` - удаление
- [x] Формы созданы (`MessageTemplateForm`)
- [x] URL patterns настроены
- [x] Шаблоны созданы
- [x] **Добавить `LoginRequiredMixin` во все views**
- [x] **Добавить фильтрацию по owner в `get_queryset()`**
- [x] **Добавить `UserPassesTestMixin` в Update/Delete views**

#### 3. Управление рассылками
- [x] Модель `Campaign` создана
  - [x] Поля: start_time, end_time, status, message_template (FK), clients (M2M)
  - [x] Поле `owner` (FK → CustomUser)
  - [x] Status choices: created, in_progress, finished
  - [x] Метод `update_status()` для динамического обновления статуса
  - [x] Метод `can_be_sent()` для проверки временного окна
  - [x] Валидация: start_time не в прошлом, start_time < end_time
  - [x] Permissions в Meta: `can_view_all_campaigns`, `can_disable_campaign`
- [x] CRUD views реализованы
  - [x] `CampaignListView` - список рассылок
  - [x] `CampaignDetailView` - детали с вызовом `update_status()`
  - [x] `CampaignCreateView` - создание (с `form_valid()`)
  - [x] `CampaignUpdateView` - редактирование
  - [x] `CampaignDeleteView` - удаление
- [x] Формы созданы (`CampaignForm` с datetime widgets)
- [x] URL patterns настроены
- [x] Шаблоны созданы с индикацией статусов
- [x] **Добавить `LoginRequiredMixin` во все views**
- [x] **Добавить фильтрацию по owner (или все для менеджеров) в `get_queryset()`**
- [x] **Добавить `UserPassesTestMixin` в Update/Delete views**

#### 4. Отправка сообщений по требованию
- [x] Создан сервисный слой (`campaigns/services.py`)
  - [x] Класс `CampaignService`
  - [x] Метод `send_campaign(campaign)` с логикой отправки
  - [x] Проверка временного окна через `can_be_sent()`
  - [x] Цикл по получателям с вызовом `send_mail()`
  - [x] Создание `CampaignAttempt` для каждого получателя
- [x] Реализована отправка через интерфейс
  - [x] Метод `post()` в `CampaignDetailView`
  - [x] Кнопка "Запустить рассылку" в шаблоне (если `can_be_sent()`)
  - [x] Форма с POST запросом
- [x] Email backend настроен (console для разработки)
- [x] **Добавить отображение статистики после отправки (messages framework)**
- [x] **Создать management команду `send_campaigns` для автоматической отправки**

#### 5. Попытки рассылок
- [x] Модель `CampaignAttempt` создана
  - [x] Поля: campaign (FK), client (FK, SET_NULL), attempt_time, status, server_response
  - [x] Status choices: SUCCESS, FAILED
  - [x] Метод `save()` с автоинкрементом `attempt_number`
- [x] Логирование попыток в `CampaignService.send_campaign()`
- [x] **Создать read-only views для просмотра попыток**
  - [x] `CampaignAttemptListView` (фильтрация по campaign)
  - [x] Отображение попыток на странице `campaign_detail.html`
- [x] **Добавить статистику попыток на detail странице рассылки**

#### 6. Главная страница
- [x] Шаблон `index.html` создан с базовым дизайном
- [x] **Добавить вычисление и отображение статистики:**
  - [x] Общее количество рассылок пользователя
  - [x] Количество активных рассылок (status='in_progress')
  - [x] Количество уникальных получателей
- [x] **Для менеджеров: статистика по всем пользователям**

---

### ЧАСТЬ 2. Расширение функциональности

#### 7. Регистрация и аутентификация пользователей
- [x] Модель `CustomUser` создана
  - [x] Наследуется от `AbstractUser`
  - [x] Поля: email, phone_number, avatar, country
  - [x] `USERNAME_FIELD = 'email'`
  - [x] Настройка `AUTH_USER_MODEL` в settings.py
  - [x] **Добавить поле `verification_code` (CharField, 6 символов)**
  - [x] **Изменить `is_active` default на `False`**
- [x] **Создать views для аутентификации:**
  - [x] `RegisterView` - регистрация с генерацией кода подтверждения
  - [x] `VerifyEmailView` - подтверждение email по коду
  - [x] `LoginView` - вход (можно использовать встроенный Django)
  - [x] `LogoutView` - выход
  - [x] `ProfileView` - просмотр/редактирование профиля
- [x] **Создать формы:**
  - [x] `UserRegistrationForm`
  - [x] `EmailVerificationForm` (не требуется - используется URL с кодом)
  - [x] `ProfileUpdateForm`
- [x] **Настроить отправку email с кодом подтверждения** (EmailVerificationService)
- [x] **Реализовать восстановление пароля (встроенные Django views)**
- [x] **Создать шаблоны:**
  - [x] `users/register.html`
  - [x] `users/verify.html`
  - [x] `users/profile.html`
  - [x] `registration/login.html`
- [x] **Настроить URL patterns в `users/urls.py`**
- [x] **Обновить навигацию в `base.html`:**
  - [x] Кнопки "Вход"/"Выход" (динамически)
  - [x] Кнопка "Регистрация" (только для гостей)

#### 8. Статистика и отчеты
- [x] **На главной странице (реализовано в п.6)**
- [x] **На детальной странице рассылки:**
  - [x] Количество всех попыток
  - [x] Количество успешных попыток
  - [x] Количество неуспешных попыток
  - [x] Таблица/список попыток с деталями (ссылка на CampaignAttemptListView)

#### 9. Ограничение доступа
- [x] **Добавить `LoginRequiredMixin` во все CRUD views**
- [x] **В `get_queryset()` фильтровать по owner:**
  ```python
  def get_queryset(self):
      if self.request.user.has_perm('campaigns.can_view_all_campaigns'):
          return Campaign.objects.all()  # Менеджер
      return Campaign.objects.filter(owner=self.request.user)  # Пользователь
  ```
- [x] **В Update/Delete views добавить `UserPassesTestMixin`:**
  ```python
  def test_func(self):
      obj = self.get_object()
      return obj.owner == self.request.user
  ```
- [x] **Создать management команду `create_groups`:**
  - [x] Создание группы "Менеджеры"
  - [x] Назначение permissions группе
- [x] **Создать views для управления пользователями (только для менеджеров):**
  - [x] `UserListView` - список пользователей
  - [x] `UserDetailView` - детали пользователя
  - [x] `ToggleUserActiveView` - блокировка/разблокировка (is_active)
- [x] **Создать шаблоны:**
  - [x] `users/customuser_list.html` (список пользователей)
  - [x] `users/customuser_detail.html` (детали пользователя)
- [x] **Добавить проверки прав в шаблоны:**
  ```django
  {% if perms.campaigns.can_view_all_campaigns %}
      <!-- Контент для менеджеров -->
  {% endif %}

  {% if campaign.owner == request.user %}
      <!-- Кнопки редактирования/удаления -->
  {% endif %}
  ```
- [ ] **Реализовать отключение рассылок менеджерами**

#### 10. Кеширование
- [x] **Настроить серверное кеширование в settings.py**
  - [x] Конфигурация CACHES (Redis или Memcached)
  - [x] Добавить переменные в .env
- [x] **Применить кеширование к главной странице (статистика)**
- [x] **Применить кеширование к списку рассылок (опционально)**

---

### ДОПОЛНИТЕЛЬНО

#### Management команды
- [x] **`send_campaigns` - автоматическая отправка рассылок**
  - [x] Находит рассылки в временном окне
  - [x] Вызывает `CampaignService.send_campaign()`
  - [x] Логирует результаты в stdout
- [x] `create_groups` (см. п.9)

#### Общие требования проекта
- [x] Проект на GitHub
- [x] `.gitignore` настроен
- [x] Зависимости в `pyproject.toml` (Poetry)
- [x] Структура соответствует Django
- [x] Приложения `users` и `campaigns` созданы
- [x] Настройки в папке `config/`
- [x] Переменные окружения в `.env`
- [x] Шаблон `.env.sample` создан
- [ ] **Проверка Flake8 (не более 5 ошибок)**

#### База данных
- [x] PostgreSQL подключен
- [x] Все модели созданы с правильными типами полей
- [x] Связи между моделями (FK, M2M) настроены
- [x] Миграции созданы и применены
- [x] Модель пользователя от `AbstractUser`
- [x] Поле авторизации - `email`

---